version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: mysql-lms
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: lms_auth_db
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
